import CheckoutForm from "@/src/features/checkout/components/CheckoutForm";

export const metadata = {
  title: "Checkout — Shome",
};

export default function CheckoutPage() {
  return <CheckoutForm />;
}
